meta {
  name: Get Recipe
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/recipes/{{recipeId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.recipeId: isDefined
  res.body.embedding: isUndefined
}
